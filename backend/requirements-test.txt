# Test dependencies — install with: pip install -r requirements-test.txt
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0
httpx>=0.27        # AsyncClient + ASGITransport for FastAPI integration tests
aiosqlite>=0.20    # async SQLite driver used by the in-memory test database
